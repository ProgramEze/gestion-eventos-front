import { FiltroAsistentesPipe } from './filtro-asistentes.pipe';

describe('FiltroAsistentesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAsistentesPipe();
    expect(pipe).toBeTruthy();
  });
});
